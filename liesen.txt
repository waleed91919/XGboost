Wir haben den ultimativen Durchbruch erzielt! Der Dual-Model-Ansatz mit den bereinigten Daten bringt +46,37% PnL und fast 69% Win-Rate. Jetzt müssen wir dieses Meisterwerk für den Live-Einsatz auf dem Server vorbereiten und den Code professionell auf GitHub sichern.

Bitte gib mir zwei Dinge:

Teil 1: Das finale Live-Bot-Skript (polymarket_live_bot.py)

    Lade BEIDE Modelle (xgboost_polymarket.pkl als Basis und xgboost_risk_manager.pkl als Meta).

    Die finale Logik: Das Basis-Modell berechnet base_prob (nur UP-Signale). Wenn base_prob > 0.580, bereitet der Risk Manager die Features vor und berechnet die meta_prob. Der Trade wird NUR ausgeführt, wenn die meta_prob > 0.60 ist.

    Behalte das Paper-Trading (Quarter-Kelly, paper_trades_log.csv, 5-Minuten Trade-Resolution via Binance API) exakt wie vorher bei.

Teil 2: GitHub Upload & .gitignore

    GitHub hat ein Dateigrößenlimit (100 MB). Unsere 8-Jahre-CSV und die .pkl Modelle sind wahrscheinlich zu groß.

    Bitte erstelle mir den Inhalt für eine .gitignore Datei, die .pkl, .csv (außer dem leeren Log-File), __pycache__ und virtuelle Umgebungen (venv) ausschließt.

    Gib mir danach die exakten Terminal-Befehle (Git Bash), um das Projekt lokal zu initialisieren, zu committen und auf ein neues GitHub-Repository zu pushen.

Bitte gib mir den sauberen Code und die genauen Schritte!